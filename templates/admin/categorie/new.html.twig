{% extends 'base.html.twig' %}

{% block title %}Ajout categorie{% endblock %}
{% block carousel %}{% endblock %}
 {% block categories %} {% endblock %}
 {% block contact %} {% endblock %}
 {% block collaborateurs %} {% endblock %}
 {% block scroll %} {% endblock %}
 {% block social %} {% endblock %}
 {% block footer %} {% endblock %}

{% block produits %}
    <div id="ajoutForm" class="my-2">
        <h3 class="text-center">Ajouter une nouvelle Catégorie</h3>

        {{ form_errors(form) }}
        {{ form_start(form) }}
            <div class="row mb-1">
                {{ form_row(form.nameCategorie) }}
            <div>
            <div class="row mb-1">
                <p>Description</p>
                <div class="col-sm-12 col-md-12" id="editor"></div>
                {{ form(form.description) }}        
            </div>

            <div class="row mb-1">
                {{ form_row(form.priorite) }}
            <div>
            
            <div class="row mb-1">
                {{ form_row(form.couverture) }}
            <div>
                <div class="row mb-1">
                {{ form_row(form.imgProduit) }}
            <div>
            
            <div class="d-flex justify-content-between">
                <button class="btn btn-primary ">{{ button_label|default('Enregistrer') }}</button>
                <a class="btn btn-secondary " href="{{ path('app_admin_categorie_index') }}">Retour</a>
            </div>
        {{ form_end(form) }}
    </div>

{% endblock %}
